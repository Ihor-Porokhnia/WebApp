name: SecretPiping

on:
  push:
    branches: 
      - testing

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Decode fullchain
      run: base64 -d ${{ secrets.FULLCHAIN }} > fullchain.pem
    - name: Out fullchain
      run: cat fullchain.pem
